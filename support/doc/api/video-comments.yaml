parameters:
  threadId:
    name: threadId
    in: path
    required: true
    schema:
      type: number
    description: 'The thread id (root comment id)'
  commentId:
    name: commentId
    in: path
    required: true
    schema:
      type: number
    description: 'The comment id'